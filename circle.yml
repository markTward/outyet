machine:
  pre:
    - curl -sSL https://s3.amazonaws.com/circle-downloads/install-circleci-docker.sh | bash -s -- 1.10.0

  services:
    - docker

dependencies:
  pre:
    - which gcloud
    - which kubectl
    - which go
    - which docker
    - docker version
    - go version
